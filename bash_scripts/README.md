# Bash Scripts

This directory contains Bash scripts for computational mineral physics projects.

## Adding a New Script

When adding a new Bash script:
1. Include a descriptive filename
2. Add a shebang line (#!/bin/bash) at the top
3. Include comments explaining what the script does
4. Make the script executable: `chmod +x script_name.sh`
